# ===========================================
# REQUIRED CONFIGURATION
# ===========================================

# Discord Bot Configuration
# Get these from https://discord.com/developers/applications
TOKEN=your_discord_bot_token
CLIENTID=your_discord_client_id
GUILDID=your_discord_guild_id
DISCORD_CLIENT_SECRET=your_discord_client_secret

# Database (PostgreSQL)
# Examples:
# - Neon: postgresql://user:password@ep-xxx.region.neon.tech/database?sslmode=require
# - Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
# - Railway: postgresql://postgres:password@containers-xxx.railway.app:5432/railway
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# DeepL Translation API
# Get from https://www.deepl.com/pro-api
DEEPL_API_KEY=your_deepl_api_key

# Web Server & OAuth
# SESSION_SECRET should be a strong random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your_random_session_secret_minimum_32_chars

# URLs
# BASE_URL: Where the Express server runs (API + OAuth callbacks)
# DASHBOARD_URL: Where the Next.js frontend runs
BASE_URL=http://localhost:4001
DASHBOARD_URL=http://localhost:3000

# Environment
NODE_ENV=development
PORT=4001

# Optional: Cookie domain for cross-subdomain authentication
# Only needed if API and Dashboard are on different subdomains
# Example: .yourdomain.com (note the leading dot)
# COOKIE_DOMAIN=.yourdomain.com

# ===========================================
# OPTIONAL - STRIPE (For Subscriptions)
# ===========================================
# Only required if you want to enable paid subscription tiers
# Get from https://dashboard.stripe.com/apikeys

# API Keys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Guild Subscription Price IDs
# Create products/prices in Stripe Dashboard
STRIPE_PRO_PRICE_ID=price_...
STRIPE_PREMIUM_PRICE_ID=price_...

# User Subscription Price IDs
STRIPE_USER_PRO_PRICE_ID=price_...
STRIPE_USER_PREMIUM_PRICE_ID=price_...
